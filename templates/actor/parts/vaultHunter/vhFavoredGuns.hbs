<section class="actor-section actor-favored-guns">
    <div class="actor-row">
        <h2 title="{{localize "SHEETS.actor.vaultHunter.favoredGun.hint"}}">
            {{localize
                'SHEETS.actor.vaultHunter.favoredGun.label'
            }}
        </h2>
    </div>
    {{#each gunTypes}}
    <div class="actor-row actor-favored-gun">
        <div class="actor-col">
            <input
                type="checkbox"
                id="{{ this.key }}"
                name="{{ this.key }}"
                {{checkInArray
                actor.favoredGuns
                this.key}}
            />
        </div>
        <div class="actor-col">
            <image src="{{ this.icon }}" title="{{localize this.name}}" />
        </div>
        <div class="actor-col">
            <label for="{{ this.key }}" title="{{ this.description }}">{{localize this.name}}</label>
        </div>
    </div>
    {{/each}}
</section>
