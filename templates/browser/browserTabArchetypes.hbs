<section class="tab col-12 {{ tab.cssClass }}" data-group="primary" data-tab="archetypes">
    <h3>Archetypes</h3>
    <div class="browser">
        <div class="sidebar">
            Sidebar

            <button type="button" data-action="addarchetype">
                <i class="fa-solid fa-plus"></i>
            </button>
        </div>
        <div class="content">
            <div class="browser-available-items badass-section">
                {{> browserAvailableItems items=archetypes}}
            </div>
        </div>
    </div>
</section>
